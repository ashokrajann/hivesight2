import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import styled from 'styled-components/native';

const LessonBar = ({lessons}) => {
  return (
    <TouchableOpacity>
      <Text>lessons</Text>
    </TouchableOpacity>
  );
};

export default LessonBar;
